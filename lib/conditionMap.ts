/**
 * conditionMap.ts — SIMPLIFIED after KB fix
 * All three KB tables now use KB1 condition_flagged names as master.
 * This file is now a direct pass-through — no translation needed.
 * Kept for future extensibility only.
 */

export function translateForSupplements(conditions: string[]): string[] {
    return conditions
  }
  
  export function translateForTherapies(conditions: string[]): string[] {
    return conditions
  }
  
  export function translateForDietary(conditions: string[]): string[] {
    return conditions
  }